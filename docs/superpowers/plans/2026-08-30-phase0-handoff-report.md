# 阶段 0 通宵执行报告（2026-08-30 晨间交接）

> 计划依据：2026-08-30-practice-editor-feature-00-baseline.md
> 状态：**阶段 0 机器侧工作全部完成，105 项测试全绿**；用户已验收基线（总体通过），三点反馈已修复并重建基线；
> 补录样题与基线标记两项拍板事项也已落实；剩余关卡：放行阶段 1。

## 1. 任务完成情况

| 任务 | 状态 | 关键产物 |
|---|---|---|
| 0.1 数据盘点 | ✅ | `scripts/phase0_inventory.py` → 3 练习/21 题/82 块，9 题已人工修改，8 处外部图引用，无坏数据 |
| 0.2 基线样本 | ✅ | 4 份样本（物理 13 题 7 页 / 数学 8 题 6 页 / 化学 9 题 6 页 / 英语 8 题 4 页），产物在 `data/baselines/`，脚本 `scripts/phase0_baseline.py` 可重跑 |
| 0.3 API 基线锁定 | ✅ | `tests/test_baseline_api_lock.py`（6 项） |
| 0.4 兼容数据模型 | ✅ | 迁移 `c5f9a2b7d3e1`、`rich_document.py` 转换器、块双写桥、新练习标 `native` |
| 0.5 旧练习迁移 | ✅ | `scripts/phase0_migrate.py`（试运行/备份/单份重试）；**真实数据已迁移：3 份/21 题/0 警告，二次执行幂等（哈希一致）** |
| 0.6 迁移回归测试 | ✅ | `test_rich_document.py`（16 项）+ `test_doc_migration.py`（9 项，含失败隔离/重试/备份）；迁移后旧练习端到端可预览/导出 |
| 0.7 视觉验收清单 | ✅ 成文 | `docs/superpowers/plans/2026-08-30-phase0-visual-acceptance-checklist.md`，**逐页验收需你本人** |
| 0.8 子页面刷新 | ✅ | SPA 回退路由 + 15 项测试 + 生产模式冒烟 |

测试演进：54 → 75 → 100 → 104 → **105 项全部通过**（验收反馈新增 4 项 + 基线标记 1 项回归测试）。

## 2. 安全措施与回退路径

- 迁移前自动备份：`data/backups/phase0_20260830_010242/`（数据库 + 练习资产）
- 迁移只写新字段 `rich_document`，旧字段 `content_snapshot`/`options_snapshot` 原样未动
- 回退方式：把 `data/backups/` 里的 `db.sqlite3` 复制回 `data/` 即可（新字段直接消失）

## 3. 用户验收结果与反馈修复（2026-08-30 晨）

用户逐页验收结论：**"整体基线还行"**，附三点反馈，均已修复并重建 4 份基线：

1. 化学基线题 24 出现 `/api/ocr-assets/…` Markdown 原文 → 题干引用迁移只认 `asset://` 的漏洞，
   `snapshot_question` 题干补过 `migrate_option_refs`；存量用 `scripts/phase0_fix_bad_refs.py` 幂等修复（初三上混合练习 题5/9 选项快照）。
2. 答题留白横线 → `render_service` `.space-line` 去掉 `border-bottom`，纯空白。
3. 题号不连续 → **否决"接受现状"**，改为创建练习时即按小节顺序从 1 连续编号。
   （推翻了晨间报告里"登记为基线现状"的建议，以用户决策为准。）
4. 基线重建：`phase0_baseline.py --rebuild`（删旧练习+资产目录后重建），页数不变（7/6/6/4），
   复验：题号全连续、坏引用 0 处、原题 24（现化学基线题 7）流程图正常渲染。
5. 数据库双备份：迁移备份 `data/backups/phase0_20260830_010242/` + 修复前备份 `data/db.sqlite3.bak_badrefs_*`。

## 4. 拍板事项落实情况（同日）

1. **补录样题**（"可以添加"）：来源"手动补录样题（公式+选项图）" 4 题（数学选项图选择/
   数学公式综合/物理公式填空/物理斜面图综合），图片 PIL 生成，`/api/ocr-assets` 形式引用走真实迁移管线，
   端到端验证全绿（脚本 `scripts/phase0_add_sample_questions.py` / `phase0_verify_samples.py`）。
2. **基线标记**（"标记"）：新增 `practices.is_baseline`（迁移 `e1b2c3d4f5a6`），4 份基线已置位；
   列表卡片显示"基线样本"标签，删除时额外警示；基线脚本每次运行幂等置位。
3. 前端已重新构建（dist），重启服务即可看到标记。

## 5. 剩余用户关卡

- **确认阶段 0 完成、放行阶段 1（WYSIWYG 编辑器地基）**——确认后即开始阶段 1 开发。
- 补录的 4 道样题已入题库，后续重建基线时选题会自然覆盖（公式题优先）。

## 6. 已知问题登记（不修复，固定为基线）

1. 真实数据公式/选项图覆盖不足——已补录 4 道样题（见第 4 节）；基线样本不含补录题，避免破坏固定基线。
   ——题库中原存 6 题 `/api/ocr-assets` 原始引用（展示用），入练习时会自动迁入资产，不动。

## 7. 阶段 1 就绪度

- 新文档结构（schema v1）已定义并双写到所有题目（旧 21 题 + 新 38 题 = 59 题全覆盖）
- 阶段 1 编辑器上线后只需反转双写方向（编辑器写 `rich_document`，回写旧字段）
- 基线样本可作阶段 1 起的回归输入（固定文件名 + 清单 `phase0_baseline_manifest.json`）
