# 阶段 0：视觉基线验收清单

> 生成时间：2026-08-30 凌晨（自动生成，待用户逐页确认）
> 对应计划：2026-08-30-practice-editor-feature-00-baseline.md Task 0.7
> 验收方式：对照 `data/baselines/` 下的分页 PNG 逐页检查（或直接打开 PDF）。

## 1. 基线样本清单

| 样本 | 练习标题 | 题数 | 页数 | 产物（data/baselines/） |
|---|---|---|---|---|
| 物理主基线 | 基线-物理-主 | 13 | 7 | `baseline_physics_main.pdf / .docx / _page01~07.png` |
| 数学代表 | 基线-数学-代表 | 8 | 6 | `baseline_math_rep.*` |
| 化学代表 | 基线-化学-代表 | 9 | 6 | `baseline_chemistry_rep.*` |
| 英语代表 | 基线-英语-代表 | 8 | 4 | `baseline_english_rep.*` |

每份样本均覆盖：标题、副标题、信息栏（姓名/班级/日期）、单题分值、总分、答题留白、
小节分页（综合/实验题小节从新页开始）、页码。机器清单见
`backend/scripts/reports/phase0_baseline_manifest.json`。

选题规则（固定、可复现）：每学科按题型配额选题，含公式/多图/长文本题优先；
脚本 `backend/scripts/phase0_baseline.py` 可重复运行（已存在则复用，只刷新配置与导出）。

## 2. 逐页检查项（每份样本都要过一遍）

- [ ] 题号、题干、选项完整，无乱码、无截断
- [ ] 图片全部显示（无裂图），顺序与原题一致
- [ ] 公式渲染正常（数学样本重点）
- [ ] 留白行数与题型匹配，不过长/不缺失
- [ ] 无漏题、无重复题、题目顺序与小节分组正确
- [ ] 分页位置合理（综合/实验小节顶头），无题目被拦腰截断
- [ ] 页码、总分、信息栏正确

## 3. 已知排版问题处理记录（2026-08-30 用户验收后更新）

### 已修复（用户验收反馈驱动，基线已重建）

1. **题号不连续 → 已改为创建时即连续编号**（用户决策 2026-08-30：
   "题号还是应该重新排列 连续"）。`create_practice_from_questions` 现在按小节顺序
   从 1 开始编号，不再保留题库原卷号。回归测试：`test_snapshot.py::test_create_assigns_consecutive_numbers`。
2. **化学基线题 24 出现 `/api/ocr-assets/…` Markdown 原文** → 根因：题干引用迁移只认 `asset://`，
   部分题库题 content 存的是 `/api/ocr-assets/<source_id>/…` 形式。修复：`snapshot_question`
   题干也过一遍 `migrate_option_refs`；存量练习用 `scripts/phase0_fix_bad_refs.py` 幂等修复（含备份）。
   回归测试：`test_snapshot.py::test_content_ocr_assets_ref_migrated_at_creation`。
3. **答题留白画横线 → 改为纯空白**（用户决策 2026-08-30："不应该题目下面出现横线应该是空白即可"）。
   `render_service` 的 `.space-line` 去掉 `border-bottom`（Word 导出本就是空段落）。
   回归测试：`test_render_service.py::test_answer_space_blank_no_lines`。

### 仍登记为基线现状（本阶段不修）

1. **真实数据几乎无 LaTeX 公式**：题库 195 题中仅 1 题含 `$` 公式；已补录 2 道公式样题缓解，
   "复杂公式"覆盖度仍受真实数据限制。
2. **真实数据无选项图**：已补录 1 道选项图样题；基线样本未包含补录题（避免破坏固定基线），
   阶段 1 用补录题/合成数据测试。

## 4. 后续阶段的基线差异规则

从阶段 1 起，任何输出与基线不一致，必须在任务说明中归类为以下之一：

- **修复**：修的是上面第 3 节登记的已知问题或新报缺陷；
- **预期变化**：产品决策明确要改的行为（需引用决策文档）；
- **回归**：非预期差异，必须修完再继续。

## 5. 用户确认结果（2026-08-30 晨）

- [x] 逐页验收 4 份样本：总体通过（"整体基线还行"），附三点反馈均已修复并重建基线。
- [x] 是否接受"题号不连续"：**否决**，创建练习时题号必须连续重排（已实现）。
- [x] 是否要手工补录含复杂公式/选项图的样题进题库（补齐覆盖缺口）——已补录（2026-08-30）：
      来源"手动补录样题（公式+选项图）" 4 题（数学选项图选择、数学公式综合、物理公式填空、
      物理斜面图综合），脚本 `scripts/phase0_add_sample_questions.py` 幂等，
      端到端验证脚本 `scripts/phase0_verify_samples.py` 全绿。
- [x] 4 份基线练习留在练习列表里，还是加标记/隐藏——**标记**（用户决策 2026-08-30）：
      `practices.is_baseline` 字段（迁移 `e1b2c3d4f5a6`），列表卡片显示"基线样本"标签，
      删除时额外警示；基线脚本每次运行幂等置位。

